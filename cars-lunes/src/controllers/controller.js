


const mostrarMensajitoController = (request, response) => {
    // Ejecutar la logica de la ruta -> Insertar un objeto en la DB (POST)

    response.send("Aca van autitos")
}


// Logica del controlador -> delegada -> services

// Respuesta al front

module.exports = {
    mostrarMensajitoController
}